# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/clay_studio

# Session Configuration (must be at least 64 characters)
SESSION_SECRET=your-super-secret-session-key-that-is-at-least-64-characters-long-for-security

# Static Files Path
STATIC_FILES_PATH=./frontend/dist

# Server Configuration
HOST=localhost
PORT=3001

# Clients Directory (where Claude Code environments are stored)
# Default: ../.clients (project root)
# Use absolute path or relative to backend directory
CLIENTS_DIR=../.clients