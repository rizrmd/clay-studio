{
  "name": "clay-studio",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "build": "bun run scripts/build.ts",
    "deploy": "bun run scripts/deploy.ts",
    "sqlx:prepare": "cd backend && cargo sqlx prepare",
    "sqlx:check": "cd backend && cargo sqlx prepare --check",
    "check": "cd backend && cargo check && cd ../frontend && npm run typecheck",
    "analysis:executor": "bash scripts/start-analysis-executor.sh",
    "analysis:executor:release": "bash scripts/start-analysis-executor.sh --release",
    "analysis:build": "cd backend && cargo build --bin analysis_executor",
    "analysis:build:release": "cd backend && cargo build --release --bin analysis_executor"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "workspaces": [
    "frontend"
  ],
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  }
}
