{
  "name": "clay-studio",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "build": "bun run scripts/build.ts",
    "deploy": "bun run scripts/deploy.ts",
    "sqlx:prepare": "cd backend && cargo sqlx prepare",
    "check": "cd backend && cargo sqlx prepare && cd ../frontend && npm run typecheck"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "workspaces": [
    "frontend"
  ],
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  }
}
